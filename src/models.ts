export interface DogModel {
  bread_for: string;
  bread_group: string;
  height: object;
  id: number;
  image: { id: string; width: number; height: number; url: string };
  life_span: string;
  name: string;
  origin: string;
  temperament: string;
  weight: object;
}
